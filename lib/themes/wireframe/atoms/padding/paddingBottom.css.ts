import styles from 'sku/tss-modules';
import tokens from '../../tokens/tokens';
import rowSpacingForCssRule from '../../../utils/rowSpacingForCssRule';

export default styles(rowSpacingForCssRule('paddingBottom', tokens));
